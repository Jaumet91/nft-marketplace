type Props = {
  inputType: string
  title: string
  placeholder: string
  handleClick: Function
}

export const Input = ({
  inputType,
  title,
  placeholder,
  handleClick
}: Props) => {
  return <div>Input</div>
}
